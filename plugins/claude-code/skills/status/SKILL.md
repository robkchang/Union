---
description: Show this project's Union membership and which other agents are online.
disable-model-invocation: true
---

Run both with the Bash tool and show the output together:

```
union info
union agents
```

If `union info` says there is no node here, tell the user to run
`/union:join`. Do not send anything.
